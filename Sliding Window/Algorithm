学习资料：https://www.zhihu.com/question/314669016

概念：  
滑动窗口算法可以用以解决数组/字符串的子元素问题，它可以将嵌套的循环问题，转换为单循环问题，降低时间复杂度。  

滑动的方式是去掉最左边的元素，添加最右边的元素，然后根据题目要求对比当前窗口与所求。
  
该算法有两类题，一类是窗口大小固定的，这类比较简单；另一类是窗口大小变化的：  
这类题，首先确定left=0，然后right向右++，直到满足条件或包含结果之后暂停，然后left再++，直到停止条件。当right，left分别++再暂停后，可以res++，这一个round结束。  
当然，这里有一些细节要注意，比如left根据什么来移动，如lc713，是每次temp/left, 然后循环/终止条件是不满足结果要求（乘积大于k），即当乘积小于k时就停止了。
